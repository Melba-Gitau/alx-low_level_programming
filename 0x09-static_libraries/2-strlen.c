#include <string.h>
/**
* _strlen -> function to get length of string
* @s: string pointer to be passed to these function
* Return: returns length of the string
*/
int _strlen(char *s)
{
	return (strlen(s));
}
